---
description: 优先使用成熟第三方框架组件与资源
globs: **/*.{gd,cs,ts,tsx,js,jsx,py,java,go,rs,c,cpp,h,hpp}
alwaysApply: false
---

# 优先第三方方案

仅在“生成/修改代码”时生效；生成文档时不生效。

- 遇到常见能力（UI 组件、网络请求、序列化、动画、寻路、状态管理）时，优先评估成熟第三方框架/组件/资源。
- 满足需求时，不重复手写同类基础轮子。
- 选型遵循：稳定维护 > 社区使用广 > 项目兼容性高 > 学习/接入成本低。
- 若项目已存在同类依赖，优先复用现有依赖，避免平行技术栈。
- 仅在以下场景自行实现：第三方无法满足核心约束、接入成本明显更高、或存在许可证/安全风险。

```ts
// BAD: 手写基础防抖实现（项目已有成熟工具）
function debounce(fn: (...args: any[]) => void, wait: number) { /* ... */ }

// GOOD: 优先复用成熟库能力
import { debounce } from "lodash-es";
```
