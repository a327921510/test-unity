# 需求完善与闭环检查 v1

## 1. 已确认要素（已定版）

### 1.1 地形与地图相关（已定版）
- 统一以 `地形与地图模块_设计_v1.md` 为唯一规则来源（地形枚举、可通行、补给可达、视野、高山阻挡等）。

### 1.2 城市与资源相关（已定版）
- 统一以 `城市发展模块_设计_v1.md` 为唯一规则来源（城市分级、人口增长、治安、耐久、月度产出、兵役/兵源转换等）。

### 1.3 补给与断粮相关（已定版）
- 统一以 `补给与后勤模块_设计_v1.md` 为唯一规则来源（运输兵补给限制、资源类型、断粮惩罚、战法点联动等）。

### 1.4 跨模块约束（引用）
- 地形可达由 `地形与地图模块_设计_v1.md` 输出，补给执行由 `补给与后勤模块_设计_v1.md` 处理。
- 城市资源与人口治安口径由 `城市发展模块_设计_v1.md` 统一定义，其他模块仅消费输出。
- 全局空间与行动硬规则以 `地形与地图模块_设计_v1.md` 与 `系统总览_模块关系图_v1.md` 为准。
- 战斗/攻城/伤员机制与事件字段口径以 `战斗与攻城模块_设计_v1.md` 为准。
- 入城拆散与回库截断口径以 `兵种与部队编制模块_设计_v1.md` 为准。
- 本文档不再重复任何已在上述模块文档定版的规则明细。

## 2. 未闭环与待定项（已冻结为 v1-default）

> 说明：以下条目已在 `规则冻结_v1.md` 给出默认值（`v1-default`），当前可直接进入 Unity 实作；后续平衡只调参数。

### 2.1 兵种与转换
- 暂无（已定版，详见 `兵种与部队编制模块_设计_v1.md`）。

### 2.2 补给与运输兵
- 暂无新增待定项（已定版，详见 `补给与后勤模块_设计_v1.md`）。

### 2.3 地形规则细化
- `已冻结(v1-default)` 水兵属性模板（MVP 默认不启用实装，保留模板位与默认值）。
- `已冻结(v1-default)` 沙地与草地分化开关（默认关闭，后续版本开启）。
- `已冻结(v1-default)` 弓兵攻击森林目标判定与可视化提示（硬阻挡 + 原因码提示）。

### 2.4 战斗与克制
- `已冻结(v1-default)` 弓兵 `1.2` 口径：按“最终伤害倍率”处理。
- `已冻结(v1-default)` 投石车普通对军伤害倍率（默认 `0.70`）。
- `已冻结(v1-default)` 战法点消耗规则（按战法类别固定消耗）。
- `已冻结(v1-default)` 战法释放失败条件（地形/士气/兵力生效；天气在 MVP 默认关闭）。

### 2.5 攻城与城市战
- 暂无新增待定项（已定版，详见 `战斗与攻城模块_设计_v1.md`）。

### 2.6 资源经济闭环
- `已冻结(v1-default)` 奖励将领忠诚提升曲线与递减机制（含上限与安全阈值）。
- `已冻结(v1-default)` 资源为 0 后系统惩罚与扣减顺序（按固定优先级执行）。

### 2.7 人口与治安
- 暂无新增待定项（已定版，详见 `城市发展模块_设计_v1.md`）。

### 2.8 伤员与恢复
- `已冻结(v1-default)` 伤员恢复受城市状态影响（和平/被入侵/围城三档系数）。

### 2.9 AI 必要规则（先不考虑AI规则，先完善游戏机制）
- `待定` AI 保护补给线和运输兵的行为优先级。
- `待定` AI 出征阈值（钱粮兵器兵源达到何值才会攻城）。
- `待定` AI 地形理解程度（是否规避骑兵劣势地形）。
- `待定` AI 投石车使用策略（优先破城还是优先打部队）。
- `待定` AI 撤退条件（断粮、战损、兵力劣势阈值）。

### 2.10 规则层新增遗漏方向（必要元素）
- 暂无新增待定项（已定版，详见 `系统总览_模块关系图_v1.md` 与 `城市发展模块_设计_v1.md`）。

---

## 3. 已冻结参数（更新）

1. 水兵属性模板（MVP 默认关闭实装，模板保留）。
2. 弓兵 `1.2` 生效口径（冻结为最终伤害乘区）。
3. 投石车普通对军伤害倍率（冻结默认值）。
4. 战法点消耗规则（按类别固定消耗）。
5. 战法失败条件（地形/士气/兵力限制口径）。
6. 忠诚奖励曲线与递减规则。
7. 资源归零惩罚顺序与行为限制。
8. 伤员恢复城市状态系数。
9. 统一参数总表与公式模板（见 `规则冻结_v1.md`）。

---

## 4. 当前结论

你已经完成了核心框架（地形、资源、兵种、补给、攻城、人口、伤员）的“方向定义”，并已补齐 `v1-default` 参数冻结。  
当前可按 `规则冻结_v1.md` 直接进入 Unity 实作；后续仅需在不改公式结构前提下做参数平衡。
